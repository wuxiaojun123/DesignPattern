<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wxj.design.pattern">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.RippleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.YahuActivity" />
        <activity android:name=".activity.RecyclerviewActivity" />
        <activity android:name=".activity.KuGouActivity" />
        <activity android:name=".activity.StickyActivity" />
        <activity android:name=".activity.ViewDragHelperActivity" />
        <activity android:name=".activity.TextColorActivity" />
        <activity android:name=".activity.WuBaAnimActivity" />
        <activity android:name=".activity.HuaweiLoadingActivity" />
        <activity android:name=".activity.MultipleMenuActivity" />
        <activity android:name=".activity.LottieActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.NestedScrollActivity" />

    </application>

</manifest>